# Staged deletion inventory

<!-- Generated by scripts/legacy_reachability.py. Do not edit by hand. -->

MotionKernel inherited a single-kernel research harness, starter kernels, example models and a KernelBench bridge from upstream. Some of it is still load-bearing; some is dead weight on a public V1. This report checks five independent reachability signals per file and proposes deletion only where *all five* say unused.

Regenerate and re-check with:

```bash
python scripts/legacy_reachability.py --write
```

Signals: imported by tracked code, reachable from the CLI entry point, named in pyproject packaging, used by tests, referenced by artifact generation or loading, documented as a command or path.

**54 files are reachable and stay. 0 are unreferenced by every signal.**

## Proposed stages

Staged so each step is independently revertible, and so a stage that turns out to be wrong is cheap to undo.

_No file is unreferenced by all six signals, so this pass proposes no deletions._

## Reachable — do not delete

| File | Why it stays |
|---|---|
| `analysis.py` | documented as a command or path |
| `bench.py` | named in pyproject packaging; documented as a command or path |
| `campaign.py` | used by tests (1); documented as a command or path |
| `discovery.py` | used by tests (1); documented as a command or path |
| `examples/custom_ops/add.py` | documented as a command or path |
| `examples/custom_ops/add_kernel.py` | documented as a command or path |
| `examples/custom_ops/affine.py` | documented as a command or path |
| `examples/custom_ops/affine_kernel.py` | documented as a command or path |
| `export_hf.py` | documented as a command or path |
| `extract.py` | documented as a command or path |
| `kernel.py` | imported by ['prepare.py']; referenced by artifact generation/loading; documented as a command or path |
| `kernelbench/__init__.py` | named in pyproject packaging; documented as a command or path |
| `kernelbench/bench_kb.py` | named in pyproject packaging; documented as a command or path |
| `kernelbench/bridge.py` | named in pyproject packaging; documented as a command or path |
| `kernelbench/scorer.py` | named in pyproject packaging; documented as a command or path |
| `kernels/__init__.py` | imported by ['kernels/cuda/cross_entropy.py', 'kernels/cuda/flash_attention.py', 'kernels/cuda/fused_mlp.py']; named in pyproject packaging; documented as a command or path |
| `kernels/cross_entropy.py` | named in pyproject packaging; documented as a command or path |
| `kernels/cuda/__init__.py` | imported by ['kernels/cuda/cross_entropy.py', 'kernels/cuda/flash_attention.py', 'kernels/cuda/fused_mlp.py']; named in pyproject packaging; documented as a command or path |
| `kernels/cuda/_compile.py` | imported by ['kernels/cuda/cross_entropy.py', 'kernels/cuda/flash_attention.py', 'kernels/cuda/fused_mlp.py']; named in pyproject packaging; documented as a command or path |
| `kernels/cuda/cross_entropy.py` | named in pyproject packaging; documented as a command or path |
| `kernels/cuda/flash_attention.py` | named in pyproject packaging; documented as a command or path |
| `kernels/cuda/fused_mlp.py` | named in pyproject packaging; documented as a command or path |
| `kernels/cuda/layernorm.py` | named in pyproject packaging; documented as a command or path |
| `kernels/cuda/matmul.py` | named in pyproject packaging; documented as a command or path |
| `kernels/cuda/reduce.py` | named in pyproject packaging; documented as a command or path |
| `kernels/cuda/rmsnorm.py` | named in pyproject packaging; documented as a command or path |
| `kernels/cuda/rotary_embedding.py` | named in pyproject packaging; documented as a command or path |
| `kernels/cuda/softmax.py` | named in pyproject packaging; documented as a command or path |
| `kernels/flash_attention.py` | named in pyproject packaging; documented as a command or path |
| `kernels/fused_mlp.py` | named in pyproject packaging; documented as a command or path |
| `kernels/layernorm.py` | named in pyproject packaging; documented as a command or path |
| `kernels/matmul.py` | named in pyproject packaging; documented as a command or path |
| `kernels/reduce.py` | named in pyproject packaging; documented as a command or path |
| `kernels/rmsnorm.py` | named in pyproject packaging; documented as a command or path |
| `kernels/rotary_embedding.py` | named in pyproject packaging; documented as a command or path |
| `kernels/softmax.py` | named in pyproject packaging; documented as a command or path |
| `kernels/wan_gated_residual.py` | named in pyproject packaging; documented as a command or path |
| `kernels/wan_gated_residual_norm.py` | named in pyproject packaging; documented as a command or path |
| `kernels/wan_modulated_layer_norm.py` | named in pyproject packaging; documented as a command or path |
| `models/__init__.py` | named in pyproject packaging; used by tests (3); documented as a command or path |
| `models/bert_base.py` | named in pyproject packaging; documented as a command or path |
| `models/custom.py` | named in pyproject packaging; documented as a command or path |
| `models/gpt2.py` | named in pyproject packaging; documented as a command or path |
| `models/llama_7b.py` | named in pyproject packaging; documented as a command or path |
| `models/wan_gated_residual.py` | named in pyproject packaging; used by tests (2); documented as a command or path |
| `models/wan_gated_residual_norm.py` | named in pyproject packaging; used by tests (2); documented as a command or path |
| `models/wan_modulated_layer_norm.py` | named in pyproject packaging; used by tests (2); documented as a command or path |
| `optimize.py` | named in pyproject packaging; documented as a command or path |
| `orchestrate.py` | documented as a command or path |
| `prepare.py` | documented as a command or path |
| `profile.py` | documented as a command or path |
| `reference.py` | imported by ['prepare.py']; used by tests (1); documented as a command or path |
| `verify.py` | documented as a command or path |
| `workload.py` | used by tests (1); documented as a command or path |

## Why nothing is proposed for deletion yet

Every inherited candidate is reachable by at least one signal, and for most of them the only signal is *documentation*: `README.md` still presents the inherited single-kernel workflow (`profile.py`, `extract.py`, `kernel.py`, `bench.py`, the starter kernels, the example models, KernelBench) as a supported way to use the project.

That makes deletion a **scoping decision, not a code fact**. Nothing can be proven unused while the README tells people to run it. The order of operations is therefore:

1. decide whether V1 still offers the single-kernel research workflow;
2. if not, remove it from the user-facing docs first;
3. re-run this report, which will then have grounds to propose stages;
4. apply each stage as its own commit, with the CPU suite, a build and a clean-install smoke test after each.

Deleting code that is still advertised would break documented commands for anyone following the README, which is a worse public-V1 outcome than shipping some inherited surface area.

## Caveats

Reachability is static. A file could still be referenced by a string in a workload manifest, a docs example, a shell script, or an external user's code. Nothing here is deleted automatically: this report is the argument, and each stage should be applied as its own commit with the full CPU suite, a build, and a clean-install smoke test run afterwards.

Deleting inherited files does not change the licensing position: the upstream MIT notice in `LICENSE` still covers what remains, and `PROVENANCE.md` continues to record what was inherited.

