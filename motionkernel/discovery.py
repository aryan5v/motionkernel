"""Canonical alias for :mod:`autokernel.discovery`.

A plain re-export, so type checkers resolve it and the objects are the *same*
objects: ``motionkernel.discovery.X is autokernel.discovery.X``.
"""

from __future__ import annotations

from autokernel.discovery import *  # noqa: F401,F403
from autokernel.discovery import __all__  # noqa: F401
